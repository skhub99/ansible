- name: loops
  hosts: localhost
  tasks:
  - name: Install and Print the Packgs
    ansible.builtin.package:
      pname: "{{item}}"
      state: present
    loop:
    - git
    - mysql
    - postfix
  