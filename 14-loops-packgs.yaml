- name: loops
  hosts: localhost
  tasks:
  - name: Install and Print the Packgs
    ansible.builtin.package:
      PackageName: "{{item}}"
      state: present
    loop:
    - git
    - mysql
    - postfix
  